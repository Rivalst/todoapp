name: todoapp
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^5.0.0
  shared_preferences: ^2.0.8
  flutter_lints: ^2.0.0
  provider: ^6.0.5
  sliding_up_panel: ^2.0.0+1
  flutter_sliding_up_panel: ^2.0.1
  cupertino_icons: ^1.0.2
  day_night_time_picker: ^1.2.0+2
  sqflite: ^2.2.8+4
  path: ^1.8.3
  timer_builder: ^2.0.0
  table_calendar: ^3.0.9
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_native_splash: ^2.3.1


flutter_native_splash:
  color: "#ffffff"
  image: assets/splash_sc.jpg

flutter:
  uses-material-design: true
  assets:
    - assets/splash_sc.jpg

